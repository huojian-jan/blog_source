---
description: 博客 Markdown 排版结构（层级、周报模板、列表与代码块规范）
globs: content/**/*.md
alwaysApply: false
---

# 博客排版规则

适用于 `content/` 下所有 Markdown。目标：层次清晰、扫读友好，风格参考成熟周报/技术博客（如分段明确、小节标题、输入清单等）。

## 标题层级

- **H1**：正文里尽量不用；Hugo 文章标题以 frontmatter 的 `title` 为准，正文从 `##` 起笔。
- **H2**：大章节（前言、核心观点、实践、总结等）。
- **H3**：章节内话题或小节。
- **H4**：仅在「输入」类清单下细分（如 收藏 / 文章 / 视频），避免再深。
- 同级标题风格一致：要么都简短名词短语，要么都带简短说明，勿混用过长句子当标题。

## 文章骨架（按需选用）

### 技术/概念文

1. `## 前言` 或 `## 背景`：一句话说清读者能带走什么。
2. `## 核心内容`：分 `###` 讲定义、原理、边界。
3. `## 实践/注意点`（可选）：清单或短段落。
4. `## 总结`：3～5 条要点或一句收束。
5. `## 参考`（可选）：外链列表。

### 周报 / 随笔 / 复盘

1. `## 前言`：时间段 + 本篇在记什么（可一段）。
2. 若干 `## 主题名`：每个主题下用 `###` 拆事件与思考，段不宜过长。
3. `## 有趣的事与物`（可选）：用 `### 输入` 再 `#### 收藏` / `#### 文章` / `#### 视频` / `#### 书籍` 等；条目用 `-`。
4. `## 小结`（可选）：收束或待办。

## 段落与空行

- 段间 **至多 1 个空行**；不要连续多个空行。
- 单段以 **3～6 行为宜**；超过则拆段或改列表。
- 金句、引用、对话可用 `>` 单独成块，前后各 1 空行。

## 列表

- 无序列表统一用 `-`，一层即可；需要层次时用子列表缩进，不要过深（≤2 层）。
- 列表前最好有一句总起；关键项名用 **加粗**。
- 并列步骤用有序列表 `1.` `2.`。

## 代码与行内格式

- 行内：`keyword`。
- 代码块：必须带语言标识（如 ` ```text `、` ```bash `、` ```csharp `），前后各 1 空行。
- **粗体**：术语、小节要点；*斜体*：轻提示；勿整段加粗。

## 链接与图片

- 链接：`[可读文案](https://...)`，避免裸链占一整段。
- 图片：`![简述](url)`，`alt` 写人话，便于无障碍与 SEO。

## 与写作风格规则的配合

- `description`、摘要、`<!--more-->` 等仍遵循 `blog-writing-style.mdc`。
- 本规则负责 **结构与小标题**；润色时优先满足层级与空行，再改句式。

## 发布前自检

- [ ] 正文无重复 H1；`##` / `###` 顺序合理、不跳级滥用。
- [ ] 代码块有语言标签；列表、引用前后空行正确。
- [ ] 长文有「前言 + 总结」或等价收束，避免戛然而止。
